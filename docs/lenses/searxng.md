# Configuring the SearXNG lens

Configuration takes place in the `~/.config/spyglass/searxng/config.yaml` file.

```yaml
ip: 255.255.255.255 # IP address for the SearXNG server
port: 8080          # Port of the SearXNG server
limit: 25           # Amount of results to display
```
